pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'embedder-pipedocs-court'

dependencyResolutionManagement {
    repositories {
        mavenLocal()
        mavenCentral()
    }
}
